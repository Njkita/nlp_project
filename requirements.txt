# Версии закреплены под драйвер CUDA 12.8 (см. README).
# vllm 0.9.2 несовместим с transformers>=4.53 (конфликт регистрации aimv2),
# поэтому transformers зафиксирован на 4.52.4.
vllm==0.9.2
transformers==4.52.4
torch>=2.6,<2.8
peft>=0.13,<0.20
bitsandbytes>=0.44
accelerate>=1.0
datasets
huggingface_hub[cli]
